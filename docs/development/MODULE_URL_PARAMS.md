# Module URL parameters (moved)

How `PageRenderer` passes **`data`** / route params to modules (including **`albumsGrid`** + `albumSource: 'current'`) is documented in **[`PAGE_BUILDER_MODULES.md`](./PAGE_BUILDER_MODULES.md) — §8**.
