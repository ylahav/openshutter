# Shared local overrides (copy to .env.local at repo root).
# Values here apply to both backend and frontend when using pnpm dev from the repo root.

# MUST be identical everywhere — used to sign/verify the auth_token cookie
AUTH_JWT_SECRET=dev-secret-change-me-in-production
