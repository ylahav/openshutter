# Development Environment Configuration
# Copy this file to env.development and update with your development values

# Authentication Configuration (SvelteKit)
AUTH_JWT_SECRET=your-development-secret-key-here

# Application Configuration
NODE_ENV=development

# Backend Configuration
# BACKEND_URL can be a full URL or will be constructed from BACKEND_PORT
BACKEND_URL=http://localhost:5000
# Or specify just the port and it will use localhost:
BACKEND_PORT=5000

# Port Configuration
# Frontend dev server port (used by vite.config.ts)
PORT=4000
# Or use VITE_PORT (Vite-specific environment variable)
# VITE_PORT=4000

# Note: Storage configuration (Google Drive, AWS S3, Local Storage) 
# is managed through the admin panel and stored in the database.
# No environment variables needed for storage providers.
