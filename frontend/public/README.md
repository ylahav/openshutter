# Static assets moved

SvelteKit serves files from [`../static/`](../static/) at the site root (`/manifest.json`, `/sw.js`, icons, etc.).

Do not add new root-level assets here; use `static/` instead.
